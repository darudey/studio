'use server';
/**
 * @fileOverview This flow has been decommissioned.
 * The coupon redemption logic was moved to a standard Next.js API route 
 * at /src/app/api/redeem-coupon/route.ts to resolve a library conflict
 * between the Firebase Admin SDK and the Genkit runtime.
 */
